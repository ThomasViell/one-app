package com.uip.oneapp

/** Bauzeit-Schalter. Kein Bedienelement — Änderung nur im Quelltext (CEO-Entscheid 06.09.2026, Welle bedienbild Z-4). */
object FeatureFlags {
    /** Einstellungseintrag + Bildschirm „Offline-Karten". AUS: Eintrag unsichtbar, Route zeigt Rückweg. */
    const val offlineMapsScreen: Boolean = false
}
